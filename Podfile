# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tableview with api' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tableview with api

  target 'Tableview with apiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tableview with apiUITests' do
    # Pods for testing
  end
pod 'Kingfisher', '~> 7.8'
end
